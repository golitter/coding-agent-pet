{
  "identifier": "default",
  "description": "Default permissions for KotoriPet. core:default already grants the core event permissions (listen/emit/unlisten), so core:event:default is intentionally omitted to avoid a redundant entry.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-outer-position",
    "core:window:allow-primary-monitor",
    "core:window:allow-scale-factor",
    "core:window:allow-start-dragging",
    "core:window:allow-set-position",
    "core:window:allow-set-size",
    "core:window:allow-set-ignore-cursor-events"
  ]
}
